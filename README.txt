To run it locally, clone the repository. CD into the myproject folder directory 
or move into the myproject folder directory using the terminal. Run the command
python manage.py runserver.

To see the screenshots of what the current go into the screenshots folder.

New possible features can be seen in Possible future features.txt